Definí una función `comienzaConA` que, al aplicarla con un string, me diga si el mismo comienza con la letra 'a'.
Por ejemplo:

```haskell
ム comienzaConA "aguja"
True

ム comienzaConA "bote"
False
```
